# JamesWibberleyUserManagement
User Management project

I started by using a dictionary since I could see this was a stretch requirement, and it would be better to design the data structure using this from the beginning.
the filename path means that the project must be run from the usermanagement directory. If that doesn't work try running it from "src"

data is currently stored in a .txt file in the data folder
